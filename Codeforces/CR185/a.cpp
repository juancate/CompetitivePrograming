#include <bits/stdc++.h>

using namespace std;

int startWith(string s) {
  int i = 0;
  while(isalpha(s[i])) {
  }
}

int main() {
  ios_base::sync_with_stdio(false);
  int n;
  cin >> n;
  cin.get();

  while(n--) {
    string s;
    getline(cin, s);
  }

  return 0;
}
